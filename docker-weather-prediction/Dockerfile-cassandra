FROM node:16
ENV NPM_CONFIG_LOGLEVEL debug

RUN mkdir /root/admin && cd /root/admin
WORKDIR /root/admin

ARG DYNAMO_ENDPOINT

RUN npm install -g dynamodb-admin

CMD  DYNAMO_ENDPOINT=${DYNAMO_ENDPOINT} dynamodb-admin
